<nav class="navbar navbar-expand-md navbar-light bg-white border-bottom shadow-sm sticky-top py-2">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-compass fa-lg text-primary"></i>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-3 mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/listings">Explore</a>
        </li>
      </ul>
      <!-- Modern Search Bar -->
      <form id="search-bar" action="/listings" method="GET" class="mx-3 d-flex align-items-center flex-grow-1" style="max-width:520px; min-width:220px;">
        <div class="d-flex align-items-center bg-white shadow-sm px-3 py-2 rounded-pill gap-2 w-100" style="border:1px solid #eee;">
          <input type="text" name="location" class="form-control border-0 bg-transparent px-2" placeholder="Where" style="min-width:90px;">
          <span class="vr mx-1" style="height:24px;"></span>
          <input type="text" name="guests" class="form-control border-0 bg-transparent px-2" placeholder="Guests" style="max-width:80px;">
          <button type="submit" class="btn btn-danger rounded-pill d-flex align-items-center px-3 ms-2" style="box-shadow:0 2px 8px rgba(255,56,92,0.08);">
            <i class="fas fa-search text-white me-1"></i> Search
          </button>
        </div>
      </form>
      <ul class="navbar-nav ms-3 mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/listings/new">Add new Home</a>
        </li>
      </ul>
      <div class="navbar-nav ms-auto d-flex align-items-center gap-2">
        <% if(!currUser) { %>
          <a class="btn btn-outline-success text-nowrap rounded-pill" href="/signup">Sign up</a>
          <a class="btn btn-outline-danger text-nowrap rounded-pill" href="/login">Sign in</a>
        <% } %>
        <% if(currUser) { %>
          <a class="btn btn-outline-danger rounded-pill px-4 py-2 fw-semibold" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
